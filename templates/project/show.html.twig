{% extends 'base.html.twig' %}

{% block title %}{{ project.title }}{% endblock %}

{% block body %}
    <section class="project-details">
        <div class="container">
            <h1>{{ project.title }}</h1>
            <img src="{{ asset('uploads/projects/' ~ project.picture) }}" alt="{{ project.title }}" style="max-width: 100%;">
            <p>{{ project.description }}</p>
            <p><strong>Publié le :</strong> {{ project.createdAt|date('d/m/Y') }}</p>
        </div>
    </section>
{% endblock %}
